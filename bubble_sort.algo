ALGORITHM Bubble_sort
VAR
    lst,num1,num2,temp
BEGIN
    for num1 in range(lst):
        for num2 in range(num1,lst):
            if lst[num1]>lst[num2]:
                temp=lst[num1]
                lst[num1]=lst[num2]
                lst[num2]=temp
    print lst
                
END