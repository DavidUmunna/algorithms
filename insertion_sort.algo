ALGORITHM insertion_sort
VAR
    key i,j, lst
BEGIN
    for i in range(1,len(lst)):
        key=lst[i]
        j=i-1
        while lst[j]>key and j>=0:
                lst[j+1]=lst[j]
                j-=1
        lst[j+1]=key
END